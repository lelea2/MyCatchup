define([
    'jquery',
    'underscore',
    'backbone'
], function($, _, Backbone){
    var Group = Backbone.Model.extend({
        initialize: function() {
        }
    });

    return Group;
});