define([
    'jquery',
    'underscore',
    'backbone',
    'models/Group'
], function($, _, Backbone, Group){
    var GroupCollection = Backbone.Collection.extend({

    });

    return GroupCollection;
});